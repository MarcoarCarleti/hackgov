spring:
  datasource:
    url: jdbc:h2:mem:hackgovtest;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  jackson:
    serialization:
      write-dates-as-timestamps: false

app:
  jwt:
    secret: "test-secret-key-test-secret-key-test-secret-key"
    expiration-minutes: 240
